//-------------------------------------------------------
// tasksmith: node javascript module starter.
//-------------------------------------------------------

"use strict";

const task = require("../tasksmith.js")

//-------------------------------------------------------
// install:
//-------------------------------------------------------
const install = () => task.series(() => [
  // todo: npm dependencies here.
])

//-------------------------------------------------------
// watch:
//-------------------------------------------------------
const watch = () => task.watch("./", () => task.shell("node app.js"))

//-------------------------------------------------------
// start:
//-------------------------------------------------------
const start = () => task.shell("node app.js")

//-------------------------------------------------------
// cli:
//-------------------------------------------------------
task.debug(task.cli(process.argv, {
  "install"   : install(),
  "watch"     : watch(),
  "start"     : watch(),
}))